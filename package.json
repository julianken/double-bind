{
  "name": "double-bind",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --sort build",
    "build:desktop": "pnpm build:migrations && pnpm extract-migrations && pnpm --filter @double-bind/desktop build",
    "build:migrations": "pnpm --filter @double-bind/migrations build",
    "extract-migrations": "tsx scripts/extract-migrations.ts",
    "dev": "pnpm --filter=@double-bind/desktop dev",
    "dev:desktop": "pnpm --filter=@double-bind/desktop tauri:dev",
    "test": "vitest run --project types --project test-utils --project migrations --project core --project desktop --project scripts",
    "test:watch": "vitest",
    "test:unit": "pnpm -r test:unit",
    "test:integration": "vitest run --project core-integration",
    "test:e2e": "pnpm --filter=@double-bind/desktop test:e2e",
    "test:e2e:full": "pnpm --filter=@double-bind/desktop test:e2e:full",
    "test:e2e:summary": "node scripts/test-summary.js",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "format": "prettier --write \"packages/*/src/**/*.{ts,tsx}\"",
    "format:check": "prettier --check \"packages/*/src/**/*.{ts,tsx}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import-x": "^4.0.0",
    "globals": "^17.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.3.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.54.0",
    "tsx": "^4.19.0",
    "vitest": "^2.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
