/**
 * Streaming utilities for mobile primitives
 *
 * Provides hooks and utilities for streaming query results with
 * progressive loading indicators and memory-efficient processing.
 */

export { useStreamingQuery, type StreamQueryFunction, type UseStreamingQueryResult } from './useStreamingQuery';
