{
  "name": "@double-bind/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@double-bind/migrations": "workspace:*",
    "@double-bind/types": "workspace:*",
    "fractional-indexing": "^3.2.0",
    "ulid": "^2.3.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@double-bind/test-utils": "workspace:*",
    "@types/better-sqlite3": "^7.6.12",
    "better-sqlite3": "^11.8.1",
    "cozo-node": "0.7.6",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
