// Example integration test for core package
// Integration tests run against real CozoDB via pnpm test:integration

describe('Core Integration Tests', () => {
  it('should verify integration test project is configured', () => {
    expect(true).toBe(true);
  });
});
